// Configuração pública do painel da equipe. Sem esses valores, só a demonstração é aberta.
// Use um projeto Firebase exclusivo da clínica. Nunca coloque credenciais de servidor aqui.
export const config = Object.freeze({apiUrl:'', firebaseApiKey:''});
